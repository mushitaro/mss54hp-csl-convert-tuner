{
    "name": "mss54hp-csl-convert-tuner",
    "version": "2.2.0",
    "private": true,
    "license": "MIT",
    "scripts": {
        "dev": "next dev -p 5054",
        "build": "next build && node scripts/build-id.mjs && node scripts/gen-sw.mjs",
        "start": "next start",
        "lint": "eslint",
        "//verify:ds2": "Checks the DS2 wire format and the three judgements built on it: framing and checksum against literals rather than only round-tripping, the seed/key login the write path depends on, the echo classification that decides between a physical fault and a buffer desync, the verify byte, and the encoding checksum a QUICK write is licensed by.",
        "verify:ds2": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-ds2.mjs",
        "//analyze": "NOT verify:*. These two replay real recorded data through the real pipeline and need that data as an argument — they answer a question about one drive, where verify:* answers a question about the code and runs on a fresh clone. Registered so they are findable and so their argument is written down somewhere other than the file itself.",
        "//analyze:rf-korr": "Replays a stored session (a directory holding log.json, session.json, binaries.json — see the file header for the wrangler query that produces them) through the real filter, VE calculation and rf_korr tuner. This is what caught the tuner deriving the correction table from samples whose gate was shut.",
        "analyze:rf-korr": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-rf-korr.mjs",
        "//analyze:rf-korr-practice": "The same replay against a BASE bin alone, using the PRACTICE drive as the log — the only place the correction's 55-80 % filling gate can be made to open on demand.",
        "analyze:rf-korr-practice": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-rf-korr-practice.mjs",
        "//analyze:rf-korr-mode": "Settles whether rf_korr belongs in the VE correction at all (adversarial objection D2). Replays one or more stored sessions and asks which expression is continuous across kl_rf_korr_rf_min — a filling threshold the engine knows nothing about, so the real air-model error cannot step there. Takes the same session directories as analyze:rf-korr; pass several to pool them.",
        "analyze:rf-korr-mode": "node scripts/analyze-rf-korr-mode.mjs",
        "//analyze:pressure-pair": "The pressure coefficient identified ACROSS two drives that traversed the range in OPPOSITE directions -- what a there-and-back drive is for, without needing one. A single drive cannot answer it: pressure moves because the car climbed or descended, so corr(ln P, time) runs above 0.92 and the within-drive slope carries every drift in the run under pressure's name. Uses only cells the intervening flash left byte-identical, detrends each cell within its own session, and lets the intercept absorb everything else that differed between the drives.",
        "analyze:pressure-pair": "node scripts/analyze-pressure-pair.mjs",
        "//analyze:session": "What one recorded drive earned, and the checks only a log can answer: the trim-neutrality verdict (is laa_f bit-exact 1.000, so STFT is the whole standing error), what LLS_ST says about which ti_load_factor branch the car actually ran, whether the run can separate ambient pressure from time at all, the VE cells written with their before/after, and the reject census over the whole 480-cell table. Same session directory as analyze:rf-korr.",
        "analyze:session": "node scripts/analyze-session.mjs",
        "//analyze:cell-gates": "Answers 'this cell has plenty of samples, why was it not written'. Walks the same grid the write path walks, through the calculator's own createGrid/accumulatePoint, and prints for every cell the statistic each of the five gates looked at against the bound it had to clear -- plus the weight-per-sample ratio, which is what makes thin-weight surprising: a sample is split bilinearly across four cells, so a count of 10 can be a weight of 0.3.",
        "analyze:cell-gates": "node scripts/analyze-cell-gates.mjs",
        "//analyze:autocorrelation": "Measures the lag-1 autocorrelation of the per-sample correction inside a VE cell, which is what AUTOCORR_FALLBACK in calculator.ts is derived from. Every precision gate divides by a sample count, and a count only means what it says if the samples are independent -- la_f_regler is a 1-2 Hz limit cycle against a 4-5 Hz log, so they are not. Cross-check: the correlation time it reports should land on the controller period.",
        "analyze:autocorrelation": "node scripts/analyze-autocorrelation.mjs",
        "//analyze:write-preview": "Exactly which bytes a flash would change, from a stored session, BEFORE it is flashed. Builds the artifact from the same base and log through writtenVeGrid and BinaryPatcher.setVETableData -- the same two calls the download and the flash make -- so a layout mistake here cannot make a wrong file look right. Compare its address list against the file the app produces: a Tune_ prefix with no kf_rf_soll bytes means the ALPHA-N toggle was off.",
        "analyze:write-preview": "node scripts/analyze-write-preview.mjs",
        "//analyze:same-base-pair": "Two drives recorded on the SAME base binary, cell by cell -- the only shape of evidence that can test what DIRECT claims for itself, that the next drive answers what this one could not. Chained sessions cannot do it: their bases differ, so a cell that moved SHOULD read differently the second time. Refuses to run when the two bases are not identical. Prints what each method would write, the paired demand with its repeatable component split from the per-drive noise, and the out-of-sample test -- does one drive's correction reduce the OTHER drive's error.",
        "analyze:same-base-pair": "node scripts/analyze-same-base-pair.mjs",
        "//analyze:rebuild-write": "Rebuilds a stored session's WHOLE flash artifact offline -- VE, the derived warmup table, the logic toggles and the checksum -- through the same calls buildPatchedBuffer makes, in the same order. On a session that has been flashed it first REPRODUCES: rebuilt sha256 against binaries.tuned, which is what makes the second use trustworthy. Then, with the derivation changed, the diff against the file now in the ECU is exactly what a re-flash would move. Optional second argument writes the .bin.",
        "analyze:rebuild-write": "node scripts/rebuild-session-write.mjs",
        "//analyze:slew": "Did the tip-in slew limiter actually bind, where, and by how much -- from a log already recorded. KF_MD_LS_KOMF says what the allowance WAS and nothing about whether any of it was spent; only MD_DYN_ST bit 6 does, and that is the difference between raising a cell and leaving it alone. Collapses the slow lane back to READS, because a few-hundred-millisecond event stamped onto sixty-four samples reads as 9.2 % of a drive when the truth is 3 of 29 reads. Prints the sampling ratio so a zero is read as the lower bound it is.",
        "analyze:slew": "node scripts/analyze-slew.mjs",
        "//analyze:idle-session": "What one recorded IDLE run says, replayed through the real tuner against the car's own BASE. The on-car counterpart to verify:idle-practice, and the only place the MODEL gate can be refuted: MockIdleBench's dutyAt IS llsTvAt, so that gate is tautologically satisfied on the bench and proves nothing there. Prints the model delta against KF_LLS_TV, the admission tally, the census with every window, the target and error from the binary's own constants, the preconditions, and what it would write. Takes a directory holding session.json, idle.json and binaries.json -- the idle samples live in the log blob's `idle` array, not in `data`.",
        "analyze:idle-session": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/analyze-idle-session.mjs",
        "//verify:inertia": "Checks the inertia estimator against a simulated engine of known J. No test framework: Node 22 strips types natively, and ts-resolve.mjs supplies the extensionless-import resolution the bundler normally does.",
        "verify:inertia": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-inertia.mjs",
        "//verify:valve-model": "KF_LLS_TV read the way lls_tv_calc reads it, and the gate that proves it on the car. This is the map the idle correction moves to: KF_LLR_QVS_GRUND is sealed because cfg_m.egas = 0x00 routes lls_tv_calc from the TORQUE path, so LLR_QSOLL has no reader. Pins this car's own table, that the lookup clamps rather than extrapolates, and that the GAIN is the map's own slope (2.25 %/(kg/h) at the idle cell) rather than an invented constant.",
        "//verify:idle-gain": "The idle campaign's gain learner and the pairing it learns from. Pins the one property a cell-delta implementation gets wrong: the pair is the duty change AT THE OPERATING POINT, which on a car idling between breakpoints is smaller than the cell change and would otherwise teach a gain too small by 1/w.",
        "//verify:idle-practice": "PRACTICE has to rehearse the ITERATION, not just the measurement. Runs the bench through a full scripted session, feeds it to the real tuner, applies the proposal to KF_LLS_TV, runs it again, and asserts the standing error SHRANK. Before the rig was rewired it could not have: it was driven by KF_LLR_QVS_GRUND, so writing the proposal changed nothing and a driver rehearsing a campaign would have learned that idle tuning does nothing.",
        "verify:idle-practice": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-idle-practice.mjs",
        "verify:idle-gain": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-idle-gain.mjs",
        "verify:valve-model": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-valve-model.mjs",
        "//verify:lls-ring": "The idle-valve ring — KF_LLS_TV to KL_AQ_ABS_LLS to kl_aq_rel_rf_fakt to kf_rf_soll and back to FR — pinned against docs/low_load_surge.md 9.8-9.11, the derivation it came from. Reproduces the hand-worked cell (950 rpm / 20 kg/h solves 40.0 to 39.0 %), the whole 16-cell inverse table, and the phase margin over Session 954's 320 rolling points (39 to 47 deg mean, the sub-30 tail 25.3 to 2.8 %). Catches the two reads that fail silently: KL_AQ_ABS_LLS is indexed by duty x 50, not duty %, so a percentage returns 0 mm² for every cell and the solver emits a table of rails; and KL_FR_INEG must come from the value run at 0xDFEA, because the catalog node 0xDFDA is the x axis and reading it gives Ki = 100 instead of 5.37. Also holds the anchor row and the rows outside the measured band still, and keeps kf_rf_soll an input.",
        "verify:lls-ring": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-lls-ring.mjs",
        "//inspect:lls-tv": "Prints KF_LLS_TV out of the vendored BIN with its axes, and answers the one question a whole-table write turns on: is every stock cell already inside K_LLS_TV_MIN..MAX, so that clamping all 130 on the way past moves none the tuner did not. That is the objection that kept the OLD target sealed. Also prints the bilinear weights at the idle this car actually holds, which is where the distributed write comes from.",
        "inspect:lls-tv": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/inspect-lls-tv.mjs",
        "verify:idle": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-idle.mjs",
        "analyze:idle": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/analyze-idle.mjs",
        "analyze:idle-practice": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-idle-practice.mjs",
        "//verify:tankvent": "Checks the tank-ventilation channels: the three DS2 offsets, and that an absent TETV reading never becomes a \"valve shut\" claim. Same runner as verify:inertia.",
        "verify:tankvent": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-tankvent.mjs",
        "//verify:tankvent-patch": "Checks the K_TE_TVTE_GA patch at the byte level: exactly one byte moves, and turning it OFF restores stock rather than no-opping.",
        "verify:tankvent-patch": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-tankvent-patch.mjs",
        "//verify:coverage": "Checks the VE evidence gate: one sample no longer moves a cell, both conditions are required, and a REJECTED cell still reports the samples it has rather than 0.",
        "verify:warmup-source": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-warmup-source.mjs",
        "verify:restore-tables": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-restore-tables.mjs",
        "//verify:convergence": "The rule that decides when SHAPE may run. SHAPE projects a surface onto a constraint set, so on a map that is still moving it makes the DRIVE'S noise smooth and monotone -- a worse artefact than the bumps it removes, because it looks deliberate. Pins that an UNMEASURED map does not read as a converged one, which is the case that would unlock it on a base nobody has driven.",
        "verify:convergence": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-convergence.mjs",
        "verify:coverage": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-coverage.mjs",
        "//verify:route": "Checks the run profiles and the campaign route: the expected rate matches the measured 244 ms/sample, preflight names exactly what is missing, and only route B carries the k_new division no car has checked.",
        "verify:route": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-route.mjs",
        "//verify:incremental": "Requires the live path to equal the batch path exactly: the resuming filter and the drip-fed grid must produce the same samples, the same census and the same map as one pass over the whole log.",
        "verify:incremental": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-incremental.mjs",
        "//verify:lambda-gates": "Checks the FR 5.01 sample gates: the full-load test reads the binary's own table (so the WOT patch disables it), a missing channel never rejects, and the drop census sums to the drop count.",
        "verify:lambda-gates": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-lambda-gates.mjs",
        "//verify:field-registry": "Checks that a log column's label is a claim it can keep: a DME channel wears the symbol its block really decodes, and a computed one is not shaped like a DME symbol.",
        "verify:field-registry": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-field-registry.mjs",
        "//verify:patch-state": "Checks what the app believes the CAR is holding. A session stores its BASE unpatched and rebuilds from it, but the log was recorded AFTER the patch went in -- and WRITE PATCH-ON rewrites KF_BZ_WDK_VL, the table readLambdaLimits reads to decide which samples were full load. So: bytesAsRun reconstructs the running calibration and is a byte-for-byte no-op with nothing armed, the full-load gate goes inert exactly when the patch is armed, the reconstruction agrees with the write path cell for cell, and armedPatchesFromHistory can answer for the sessions tuneSettings cannot describe.",
        "verify:patch-state": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-patch-state.mjs",
        "//verify:save-state": "Checks that SAVE reports what it did: a stored tune says SAVED and cannot be pressed again, an outstanding one says SAVE, and no label grows past two words.",
        "verify:save-state": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-save-state.mjs",
        "//verify:flash-state": "Checks the FINAL verdict: a flash only puts a tune on the road if it carried one, and TANK VENT counts as a patch that has to come back off.",
        "verify:flash-state": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-flash-state.mjs",
        "//verify:fastentry": "Checks the FAST READ preservation plan: what must survive an erase of the Free Identifiers sector, including the flash counter and the 0x4884 boot vector, and that an un-enumerable plan refuses rather than guesses.",
        "verify:fastentry": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-fastentry.mjs",
        "//verify:session-wire": "Checks that a session survives the round trip to the store and back: the raw inertia samples are not dropped on the way out or deleted on the way in, nulls stay nulls, and sessions synced in the older bare-array shape still restore.",
        "verify:session-wire": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-session-wire.mjs",
        "//verify:preview-notice": "The preview says what it sends, and why, before it sends any of it -- and production's first-run dialog is exactly what it was. Renders the dialog in both languages and both builds: production and staging against markup pinned from before the notice existed (scripts/fixtures/disclaimer-production.*.html), the preview for every part of the notice and a policy link to #preview. Then drives every send path with the dialog out of the picture: no session, no error record and no outbox flush before preview-notice:v1 is written, the record kept in the outbox meanwhile and sent after; an old \"don't show again\" never standing in for the notice; storage that throws reading as not seen; production and staging writing no new key and sending nothing.",
        "verify:preview-notice": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-preview-notice.mjs",
        "//verify:log-bridge": "Every channel a VE exchange claims to provide is named in the object literal in page.tsx that turns a LiveMeasurement into a LogDataPoint. That literal is the ONLY bridge between the link and the log, and a channel missing from it is decoded on every sample and thrown away -- silently, with no column and no error. It has cost two drives: the long-term trim stores on #922 and the slew-limiter channels on #930. Reads the source text, because the literal sits inside a callback inside a React component with no seam to call; the extraction fails loudly rather than finding nothing.",
        "verify:log-bridge": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-log-bridge.mjs",
        "//verify:ve-divisor": "Checks that the tuned VE derivation only divides by a correction the DME will actually apply: a gate-shut sample keeps its whole trim, a gate-open one is divided by the table value at its delta, and neither an 'as-logged' session nor the conservative route divides at all. Also pins what the 15 % tuned-vs-nominal guard really tests.",
        "verify:ve-divisor": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-ve-divisor.mjs",
        "//verify:patcher": "Checks every writer at the byte level, and above all what 'restore' restores: turning a patch off puts back the bytes the binary was loaded with, not a reference car's, and falls back to a stated stock value only when the loaded binary was already patched. Also the clamps, the refusals, and that the checksum correction is idempotent.",
        "verify:patcher": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-patcher.mjs",
        "verify:compose": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-compose.mjs",
        "verify:features": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-features.mjs",
        "verify:shape-repair": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-shape-repair.mjs",
        "verify:projection": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-projection.mjs",
        "verify:ti-factor": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-ti-factor.mjs",
        "verify:trim-store": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-trim-store.mjs",
        "verify:model-gate": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-model-gate.mjs",
        "//preview": "Cloudflare Pages preview target — see wrangler.jsonc. Production is untouched: it stays on GitHub Pages from main.",
        "preview": "npm run build:preview && wrangler pages dev",
        "//deploy:preview": "The DEVELOPMENT branch, behind the owner gate, from a published commit. Refuses a dirty tree, a missing or drifted gate, a tree unfit to publish, and a commit whose source is not on GitHub's preview branch yet (it prints the push). --branch main lands on the preview project's bare apex. See scripts/deploy-preview.mjs.",
        "deploy:preview": "node scripts/deploy-preview.mjs",
        "//publish:preview": "Copies feat/develop's COMMITTED tree onto the local preview branch as one single-parent commit named \"preview: <sha>\", minus NOT_FOR_PUBLIC (archive/, .claude/, CLAUDE.md, the two docs that say where the VINs are), checks it as a fresh clone would, and stops before pushing. The preview serves only source that is public. See scripts/publish-preview.mjs.",
        "publish:preview": "node scripts/publish-preview.mjs",
        "//gate:verify": "Is functions/_owner-gate (and the client in src/lib/session-sync/owner-sync.ts) byte-identical to tsunagi-m3/tools/owner-gate, does the middleware build the gate, and is .dev.vars ignored. NOT verify:* on purpose: those run against main on every release, and main carries no gate.",
        "gate:verify": "node scripts/gate-verify.mjs --functions functions --client src/lib/session-sync/owner-sync.ts",
        "//check:public-tree": "Nothing tracked that a public repository must not hold: BMW-derived binaries, third-party XDFs, databases, secrets, a real VIN. Deliberate exceptions are listed, with reasons, in .public-tree-allow. Run by .githooks/pre-commit, by CI, by release-to-main and by publish-preview.",
        "check:public-tree": "node scripts/check-public-tree.mjs",
        "//hooks:install": "Points git at .githooks (pre-commit: check:public-tree on what is staged; pre-push: only main and preview may leave). An absolute path, so every worktree of this repository keeps the push guard. See scripts/hooks-install.mjs.",
        "hooks:install": "node scripts/hooks-install.mjs",
        "//deploy:staging": "MAIN, unmodified, on its own project — the release candidate. See scripts/deploy-staging.mjs.",
        "deploy:staging": "node scripts/deploy-staging.mjs",
        "//ship": "ONE fix, BOTH environments. preview -> release onto main -> 43 verify suites -> tsc -> commit -> staging -> read both URLs back. Requires a release note: npm run ship -- -m \"...\". Exists because \"which environment does this go to\" was a decision, and a decision nobody makes cannot be got wrong. See scripts/ship.mjs.",
        "ship": "node scripts/ship.mjs",
        "//release": "Copies feat/develop onto main as one commit, minus the deployment layer. See scripts/release-to-main.mjs.",
        "release": "node scripts/release-to-main.mjs",
        "db:migrate:local": "wrangler d1 migrations apply mss54hp-tuner-runs --local",
        "db:migrate:remote": "wrangler d1 migrations apply mss54hp-tuner-runs --remote",
        "db:sessions": "wrangler d1 execute mss54hp-tuner-runs --remote --command \"SELECT id, datetime(synced_at/1000,'unixepoch') AS synced, label, status, point_count, has_tune, has_rf, has_egt FROM sessions ORDER BY created_at DESC LIMIT 20\"",
        "db:diagnostics": "wrangler d1 execute mss54hp-tuner-runs --remote --command \"SELECT datetime(created_at/1000,'unixepoch') AS at, kind, CASE completed WHEN 1 THEN 'ok' ELSE 'FAILED' END AS outcome, exchanges, elapsed_ms, baud, requested_baud, round(median_turnaround,1) AS turnaround, round(median_total,1) AS total, round(median_host_gap,1) AS host_gap, retries, CASE mock WHEN 1 THEN 'mock' ELSE transport END AS via, substr(coalesce(error,''),1,60) AS err, id FROM diagnostics ORDER BY created_at DESC LIMIT 30\"",
        "build:preview": "next build && node scripts/build-id.mjs && node scripts/brand-preview.mjs out PREVIEW && node scripts/check-branding.mjs out PREVIEW && node scripts/gen-sw.mjs",
        "verify:wot-fuel": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-wot-fuel.mjs",
        "verify:charge-temp": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-charge-temp.mjs",
        "verify:rf-pt-korr": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-rf-pt-korr.mjs",
        "//verify:high-load-clock": "The settle clock the DRIVER sees and the filter drops samples with, as one state machine. Pins the dip rule above all: a pull that eases below 55 %RF at nine seconds and comes back has banked nothing, and a readout that kept counting through that would send someone home believing they had the samples. Also pins that an honest pull climbing at its own ~1.7 %RF/s never trips the re-stab reset, which is the false positive that would make the readout unwatchable.",
        "verify:high-load-clock": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-high-load-clock.mjs",
        "verify:settle": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-settle.mjs",
        "verify:drive-split": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-drive-split.mjs",
        "verify:spotcheck": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-spotcheck.mjs",
        "verify:production-log": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-production-log.mjs",
        "verify:slow-lane": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-slow-lane.mjs",
        "//sync:cal-catalog": "Re-vendors public/data/calibration-graph.json from the disassembly-notes repo's graph.json, stripping the TERRA-decoded values (the tab decodes the loaded binary instead). Strip-only; refuses to shrink the shipped catalog.",
        "sync:cal-catalog": "node scripts/sync-calibration-catalog.mjs",
        "//verify:cal-catalog": "Checks the vendored calibration graph and its adaptation: pinned counts (2529 params, 1705 funcs, 71 categories), every value run inside the 64 KB partial and clear of both checksum slots, bank agreeing with address, curve/map geometry consistent, the four params starting AT 0x4000/0xC000 not falsely refused, and the read-only set pinned by name.",
        "verify:cal-catalog": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-cal-catalog.mjs",
        "//verify:cal-math": "Checks the XDF math evaluator: every distinct equation in the artifact compiles (k-linked pinned as the exception), ^ is exponentiation binding tighter than / (x/2^14 at 16384 -> 1.0), round-trip accuracy of the synthesized inverses, reciprocal direction inversion, and the per-cell error policy at 5.12/x raw 0.",
        "verify:cal-math": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-cal-math.mjs",
        "//verify:cal-edits": "Checks the calibration edit path at the byte level: an edit moves exactly its run's bytes (axes and size headers untouched), signed raws round-trip two's-complement, conflicts with armed table writers are skipped and reported, managed and sealed spans are refused even when apply is called directly, reference copy is byte-exact, and buildPatchedBuffer with calibrationEdits equals the manual patcher sequence.",
        "verify:cal-edits": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-cal-edits.mjs",
        "verify:cal-logic": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-cal-logic.mjs",
        "//verify:cal-decomp": "Checks the vendored decompiler corpus and the copy that describes it: 644 texts bijective with the nodes claiming hasCode, the three populations pinned (534 parsed / 110 text-only / 1061 never decompiled) and every one of them named or unnamed as that story requires, every tokenized line concatenating back to Ghidra's own text, all 515 calls-by-address resolving to a real function, a text-only function listed as quoted C with the corpus and falling back without it, the reason each empty box gives matching whether its text exists, and the tab's strings quoting the numbers this file measured rather than the merged 1,171 they used to.",
        "verify:cal-decomp": "node --experimental-strip-types --import ./scripts/ts-resolve.mjs scripts/verify-cal-decomp.mjs",
        "//verify:cal-docs": "Checks that what docs/ecu-logic cites still exists: every function address and ECU symbol in the prose resolves to a node in the artifact (or to a position inside a function body, for an instruction-level citation), and every [C bank/addr] evidence tag names a decompiled text that is really there. It does NOT check that a document is right — a machine can tell whether a citation points at something, only a person can tell whether it supports the sentence. See docs/ecu-logic/91-evidence.md.",
        "verify:cal-docs": "node scripts/verify-cal-docs.mjs",
        "//verify:release-scope": "The release and preview exclusion lists hold what they must: every deploy tool and the backend off main, every package script that names them stripped, nothing main keeps importing a file main drops, and the preview publishing its backend while leaving the VIN-bearing paths behind.",
        "verify:release-scope": "node scripts/verify-release-scope.mjs"
    },
    "dependencies": {
        "clsx": "^2.1.1",
        "framer-motion": "^12.23.26",
        "lucide-react": "^0.562.0",
        "next": "16.1.1",
        "papaparse": "^5.5.3",
        "plotly.js": "^3.3.1",
        "react": "19.2.3",
        "react-dom": "19.2.3",
        "react-plotly.js": "^2.6.0",
        "tailwind-merge": "^3.4.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^5.20260811.1",
        "@tailwindcss/postcss": "^4",
        "@types/node": "^20",
        "@types/papaparse": "^5.5.2",
        "@types/plotly.js": "^3.0.9",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "eslint": "^9",
        "eslint-config-next": "16.1.1",
        "tailwindcss": "^4",
        "typescript": "^5",
        "wrangler": "^4.120.1"
    }
}
