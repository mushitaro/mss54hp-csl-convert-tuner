# 85 — 軽量フライホイール：慣性の実測と自動チューニング

用語は `00-glossary.md`。ティップイン／ティップアウトの機構は `45-drivability-filters.md`、
SMG 側は `80-smg-shift-tuning.md`。慣性低下が壊すもの全体は
`CSL_0401_Binary_Disassembly_Notes/docs/lightweight_flywheel_tuning.md`。

**値の出典**: `Full 211323000401PD31_TERRA.bin`。**あなたの BIN の数値は違いうる。**
本文書の手順は最初に必ず ECU を READ し、**すべての現在値をあなたのイメージから読む**ように出来ている。

**アドレス表記**: XDF アドレス。ツールが編集する 64 KB パーシャル BIN ではそのままオフセット
（slave `0x0000–0x7FFF`／master `0x8000–0xFFFF`）。1 MB フルコンテナでは slave が `0x88000 + addr`。

---

## 0. この文書が答えること

軽量フライホイールに替えたあと、**慣性比 `r = J_new / J_old` を実測し、
そこから各パラメータの補正量を機械的に導く**ための手順。

既存の 2 文書はどちらも正しいが、繋がっていなかった:

- `lightweight_flywheel_tuning.md` — 慣性比 **0.82 を仮定**して 7 制御ドメインの変更セットを出す。
  比そのものは推定のまま。
- `80-smg-shift-tuning.md` §3.5 — `K_SMG_J_MOTOR` の機構を逆アセンブルで確定。

足りないのは「比を測る」部分と、「**測った比をどこに掛けるか**」の判断である。
後者のほうが重要で、間違えると 4〜20 倍ずれる（§3）。

---

## 1. 純正の J は測らなくてよい — ROM に書いてある

| 定数 | XDF | bank | 幅 | スケール | 純正値 |
|---|---|---|---|---|---|
| `K_SMG_J_MOTOR` | `0x280A` | slave | 8 bit | `X/128` | **0.25 Nms²** |
| `K_MD_J_MOTOR` | `0x9554` | master | **16 bit** | `X/268` | **0.268657 Nms²** |

**∴ 交換前のデータログは要らない。** BMW 自身が J_old を較正値として書いている。
測るのは **J_new の絶対値だけ**で、比は割り算で出る。

2 つが 7 % 違うのは**単位系ではなくエンコードの違い**（どちらも Nms²）。
BMW が自分自身と食い違っているだけなので、**比はそれぞれ自分の純正値に対して取る**。

> **`K_MD_J_MOTOR` は 16 bit である。** `0x9554` のバイトは `00 48` で、raw 72 が 2 バイトに跨がる。
> 8 bit として読むと上位バイトを拾って**きれいな 0** が返り、
> そのまま計算すると実効慣性比が全ギアぴったり 1.0 になって
> **「補正不要」という正しそうな答えに化ける**。実装ではここを明示的に弾いている。

物理的な裏取り: S54 の DMF ≈ 10.5 kg、回転半径 `k ≈ 0.115 m` なら `J ≈ 10.5 × 0.115² ≈ 0.139`。
残り（クランク＋等価往復系＋ダンパ）≈ 0.11。合計 ≈ 0.25 で較正値と一致する。

---

## 2. J_new の測り方 — 停車ニュートラル・ペダルスイープ回帰

### 2.1 原理

停車・ニュートラルではエンジンがプラントの全部になる:

```
M_ind(n) = J·α + M_loss(n)          α = dω/dt [rad/s²]
```

回転数を 500 rpm のビンに分け、**ビンごとに `md_ind_opt_korr` 対 `α` を線形回帰**する。
**傾きが J、切片が M_loss(n)。**

ビン分割が効いているのは、**ビン内では M_loss が定数なので切片に落ちる**点である。
摩擦・ポンプ損失のモデルが一切要らない。

**そして J は回転数に依存しない。** だから**ビン間で J が一致すること自体が手順全体の検証になる**。
ばらついたら測定が壊れている（後述の `jSpread`）。

### 2.2 なぜ「停車」なのか

ティップイン／ダッシュポットのスルーレートリミッタは
**`K_MD_DF_VMIN` = 3 km/h 未満でバイパスされる**（`45-drivability-filters.md` §1）。
つまり停車していればトルク要求は素通しで、`md_ind_opt_korr` はフィルタのランプではなく
**エンジン自身の曲線の上に乗る**。変数をモデル化するのではなく消している。

それでもサンプルごとに `md_dyn_st` を見る。「バイパスされるはず」と「バイパスされた」は別の主張である。

### 2.3 操作

- ブロック **83 単独**でログ（`inertia` プロファイル）。理由は §5.1
- 暖機後、ニュートラル、停車
- ペダル **約 15 / 25 / 35 %** で 2000 → 4500 rpm を各 2 回
- 最後にニュートラルで 5000 rpm まで吹かして離し、**燃料カット中の 5000 → 3000 rpm** を 1 回（検算用）

> **全開ブリップにしないこと。** 軽量フライホイールの無負荷全開は簡単に 5000 rpm/s を超え、
> `d_n40` が飽和する（§4.1）。部分開度で測るのは我慢ではなく必要条件。

### 2.4 検算 — 無負荷自由降下

燃料カット中は燃焼トルクがゼロなので `J·α = −M_loss(n)`。
回帰から出た `J` と `M_loss` が予測する到達時間と、実測を照合する。

> **この検算が見つけられないもの**: `md_ind_opt_korr` にスケール誤差 `k` があると
> **J も M_loss も同じ `k` 倍**になり、この式では `k` が約分されて消える。
> §4.3 参照。

---

## 3. ★ 測った比をどこに掛けるか — クラス分け

**これが本文書の中心。全部に同じ係数を掛けてはいけない。**

| クラス | 見ている慣性 | 係数 |
|---|---|---|
| **A** | エンジン単体（クラッチ切・N・アイドル） | **× r** |
| **B** | エンジン＋車両（ギヤが入っているときのみ作動するもの） | **× r_eff(gang)** |
| **C** | J が ECU の式の分母そのもの | **J から直接** |
| **D** | 慣性効果ではない | **係数なし** |

### 3.1 ★ クラス B — ギヤが入っていると比は 0.82 ではない

`KF_MD_LS_KOMF` / `KF_MD_DASHPOT*` は 3 km/h 未満でバイパスされる。
**∴ ギヤが入って走っているときしか効かない。** そのときのプラントは `J_eng + J_fz(gang)`。

`KL_MD_JFZ_GANG`（master `0x955E`、y `0x956E`、16 bit、`X/268`）＝ **BMW 自身の
クランク軸換算車両慣性**:

| gang | 0 (N) | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|---|
| `J_fz` [Nms²] | 0.0 | **0.701** | **1.601** | **3.399** | **5.0** | **8.0** | **12.0** |

`r_eff(g) = (J_new + J_fz) / (J_old + J_fz)`、J_old = 0.2687、r = 0.82 のとき:

| ギア | **r_eff** |
|---|---|
| N / 停車 | **0.820** |
| 1 | **0.950** |
| 2 | **0.974** |
| 3 | **0.987** |
| 4 / 5 / 6 | **0.991 / 0.994 / 0.996** |

> **`lightweight_flywheel_tuning.md` §1.1 の `KF_MD_LS_KOMF` × 0.82 は、
> 慣性の議論としては 4〜20 倍の過補正である。**
> 正しくは 1 速 ×0.95、2 速 ×0.97、3 速以上はほぼ ×1.00。

**さらに: `KF_MD_LS_KOMF` にはギア軸が無い**（軸は `md` × `n`）。
ギア依存は**別テーブル `0x927C`（段別係数、XDF 未定義）**にある。
∴ **r_eff は rpm×負荷マップではなく段別係数テーブルに掛けるのが軸的に正しい。**
rpm×負荷マップはログが指した症状セル用に温存する。

駆動系諸元も全部 slave にあるので検算できる:
`K_SMG_I_GANG_1..6`（`0x28CD`–`0x28D2`、`X/60`）= 4.2333 / 2.5333 / 1.6667 / 1.2333 / 1.0 / 0.8333、
`K_SMG_I_HA`（`0x28CC`）= 3.6333、`K_SMG_R_RAD_DYN`（`0x280C`、16 bit、`X/10`）= 307 mm。
質量 1450 kg で手計算すると 2–3 速が BMW の表と一致する。

### 3.2 ★ クラス C — `K_SMG_J_MOTOR` は物理定数ではなく「つまみ」

機構は `d2 = min(指令スルーレート, T / K_SMG_J_MOTOR)`（`80-smg-shift-tuning.md` §3.5）。
唯一の消費者 `smg_shift_deltaN_request_update`（slave `0x038314`）の**呼び出し元は 2 つ**:

- `smg_shift_reg_prestep_flatten_profile`（slave `0x0377AE`）
- **`smg_shift_phase3_clutch_reengage_regulator`（slave `0x037C6A`）** ← クラッチ再係合

**∴ ブリップだけでなく、発進と変速のたびに効く。**
0.25 → 0.20 は再係合レギュレータの許容スルーレートを **+25 %** にする。

> **重要な反転**: 0.20 は物理値としてはむしろ正しい（ΔJ ≈ −0.045 → 0.205）。
> つまり**「実物理値に合わせたこと」自体が症状を作りうる**。
> 純正 0.25 はレートクランプとして意図的に保守的で、較正の他の部分はその平滑化を前提に
> 合わせ込まれている。
> **∴ このパラメータは「正確にすべき物理定数」ではなく「再係合の穏やかさのつまみ」として扱う。**

症状が **発進直後・低速からの踏み込み**／**変速直後の踏み足し**なら、
どちらも再係合イベントなので**まずここを純正へ戻す 1 行の A/B が最短**。

### 3.3 クラス D — 慣性では説明できないもの

| 対象 | なぜ係数が無いのか |
|---|---|
| **DMF ダンパの喪失** | 慣性ではなく**バネと減衰**。駆動系シャッフルモードが +8〜10 % 上がり、減衰要素が消える。どんな比を掛けても戻らない。**4–10 Hz は DS2 では測れない**（block 83 単独でも Nyquist ≈ 5 Hz）。加速度計以外に手段が無い |
| 失火しきい値 | 本物の失火の偏差も健全時のばらつきも**同じ 1/J** で増える。「慣性比のぶん上げる」は論法として成立しない |
| ARF | `K_AR_TMOT_MIN` = 130 °C の到達不能ゲートが残り、`MD_AR` の読み手が復元コードに存在しない |

---

## 4. 測定側の制約 — 正直に書く分

### 4.1 ★ `d_n40` の打ち切り誤差は非対称で、ゼロは二重幅

DME は `D_N40 = (D_N_SEGMENT + 0x14) / 0x28` を **0 方向へ切り捨て**て計算する。
0 方向への切り捨ては正で floor、負で ceil なので、`trunc((x+20)/40) = k` を解くと**3 つ**の場合が出る:

| k | 真値の範囲 | 幅 | 中点 |
|---|---|---|---|
| k > 0 | `[40k−20, 40k+20)` | 40 | `40k` ＝ 報告値 |
| k < 0 | `(40k−60, 40k−20]` | 40 | `40k−40` ＝ 報告値 − 40 |
| **k = 0** | **`(−60, 20)`** | **80** | **−20** |

**∴ 3 つの結論**:

1. **正側は既に不偏。** 普通の四捨五入である。「丸め誤差だから両側補正」は**正側に無い偏りを作る**
2. **負側は 1 LSB 高い。** 真値 −139 rpm/s が **−80** と報告される（逆アセンブル注記の実例）。
   補正して −120、これが −80 が示す区間の中点
3. **ゼロは −60〜+20 rpm/s の二重幅の不感帯。** 両符号が `k = 0` に潰れるので、
   報告値 0 は「加速していない」ではなく**「−20 を中心に 80 rpm/s ぶん分からない」**

3 番目は k = 0 の場合を書き下すまで式からは見えない。実装では補正後の不確かさを
原点で倍にして返している。

### 4.2 その他

- **飽和**: `d_n40` は符号付きバイトなので **+5080 / −5120 rpm/s** で張り付く。
  レールの値は「それ以上」と区別できないので**両端とも棄却**する
- **2 系統の勾配を突き合わせる**: 補正済み `d_n40`（DME の値、サンプルごと）と
  `n40` の中心差分（粗いが不偏、窓平均）。**壊れ方が無関係なので、
  一致を要求すると片方が嘘をついているサンプルだけが落ちる**
- **ブロックは 83 単独**。DS2 は request/response なので、複数ブロックを選ぶと
  同じサンプルに別の瞬間の値が混ざる。2000 rpm/s で 110 ms のブロック間差は **220 rpm** の
  ずれになり、トルクと勾配を回帰する計算にとっては致命的
- **サンプルレートは実測して記録する。** 理論値を書かない

### 4.3 ★ この手順が原理的に見つけられない誤差

`md_ind_opt_korr` は**モデル出力**であって測定値ではない。DME が実測充填と仮定効率から計算している。
モデルに乗算的な誤差 `k` があると:

- 傾き（J）も切片（M_loss）も**同じ `k` 倍**になる
- **自由降下の検算でも `k` は約分されて消える**

測定領域はこの `k` を小さくするように選んである — 暖機後・閉ループ・ペダル 15〜35 %・低排気温なので、
λ は閉ループで 1 に補正され、`rf_korr` は恒等列に乗る。
だが**測っているわけではない**。

押さえられるのは **`M_loss(4000 rpm)` が S54 のモータリングトルク（およそ 20〜60 Nm）に
収まるか**という物理的な事前分布だけで、これは `k` を ±30 % 程度にしか縛れない。

> **∴ `J_new` の絶対値は「モデルが正しければ」付きの数値である。**
> 各パラメータの最終値は**ログでの A/B が決める**。提案値は出発点であって答えではない。

---

## 5. 既存 VE チューニングとの切り分け

### 5.1 ログは型で分ける

2 つのログは互いに使えない:

| | VE ログ {3,19} | 慣性ログ {83} |
|---|---|---|
| 持っている | `rf`, `aq_rel`, `tabg`, `tmot`, `la_f_regler1/2` | `n40`, `d_n40`, `md_ind_*`, `md_dyn_st`, `gang`, `s_krafts`, `sa_we_st` |
| 欠けている | 慣性側の全部 | **`tabg` も `la_f_regler` も `aq_rel` も無い** |

実装では**別の型**にしてある（`LiveMeasurement` と `EgasMeasurement`）。
フラグではなく型なので、取り違えは**コンパイルエラー**であって
「もっともらしい間違った答え」にはならない。

### 5.2 ★ イメージの切り分け — ここだけが本物の競合

**書き込みは常に 65536 バイト全体である。**
`writePartialBinInner` はそれ以外の長さを拒否し、データ領域全体を消去してから書き直す。
**∴ 領域による住み分けは成立しない。**

```
BASE_A ─┬─ VE セッション    → TUNED_VE     （Alpha-N だけ変更）
        └─ 慣性セッション   → TUNED_INERTIA（Momentenmanager/SMG だけ変更）

TUNED_VE を書き込み → 車は VE 済み
TUNED_INERTIA を書き込み → 64 KB 全上書き → ★ VE の変更が完全に消える
```

**アドレスが 1 バイトも重なっていないのに全部消える。**
しかも両方のセッションが「自分は少ししか変えていない」と表示するので、**差分を見ても気づけない。**

対策は書き込み直前の**系譜チェック**:
ECU が保持している 2 つの CRC（slave `0x3FFC` / master `0xBFFC` の格納値）を読み、
セッションの BASE のそれと突き合わせる。**8 バイト、2 交信**。
64 KB 再読み込みは 9600 baud で約 123 秒かかり、**毎回のフラッシュを倍にする安全チェックは
やがて切られる**ので、この形にしてある。

残る риск は CRC 衝突（ペアで約 2³²分の 1）。
「間に別のものを焼いたか」という実際の問いには十分で、偽造への防御ではない。

正しい進め方は**直列**:

```
フラッシュ → 再 READ → その READ を BASE に次のセッション
```

---

## 6. 手順

| # | フェーズ | 車 | フラッシュ | 内容 |
|---|---|---|---|---|
| **P0** | BASE 読み取り | 停車 | — | ECU から 64 KB を READ。全対象の現在値を復号。以後すべての系譜の起点 |
| **F1** | `K_SMG_J_MOTOR` → 0.25 | — | 1 回 | **最初にやる。** 症状の第一容疑者を純正へ戻すだけ。J の実測を待たない |
| **V1** | F1 の A/B | 走行 | — | 発進・低速踏み込み・変速直後の踏み足しを各 5 回 |
| **P1** | 適応値クリア | 停車 | — | `md_llra` は τ=15 s・1 走行サイクル最大 1.0 Nm。**残っていると以後の A/B が静かに再ベースライン化する** |
| **P2** | J 実測 | 停車 N | — | §2.3 の操作。SAVE してから次へ |
| **P3** | 推定・提案 | — | — | `J ± σ`、`r`、`r_eff(g)`。不合格なら P2 へ戻る |
| **F2** | クラス C ＋ A | — | 1 回 | `K_MD_J_MOTOR`、`K_LFR_TAU_IA1`/`_IA2_KKS`、`KL_SA_N40_GANG`[0]/`_HYS_GANG`[0]、`K_N_TAU_DN`、`K_SMG_N_ZIEL_ABWUERG` |
| **F3** | 下向き権限（単独） | — | 1 回 | `KL_MD_RES_LRW` y[0] ＋ `KL_LFR_TZ_NEG`。**ペア必須**かつ根拠が `xref-only` なので他と束ねない |
| **F4** | クラス B ＋ D | — | 1 回 | 段別係数 `0x927C`/`0x929E`（XDF 未定義の直接パッチ）＋ ログで特定した `KF_MD_LS_KOMF` の症状セル |
| **F5** | 軸移動（最後・単独） | — | 1 回 | `KL_SA_N40_DN40` の x 軸。**他と絶対に束ねない** |

### 束ねてよい / いけない

判定基準は**検証信号が重なるか**。

**F2 は束ねてよい** — 検証信号が全部別（再係合の `n40` オーバーシュート／アイドル復帰時間／
`sa_we_st` の二段しゃくり／勾配／発進回転）で、1 回のログで独立に読める。かつ全部 `code-confirmed`。

**F3 は束ねてはいけない** — 根拠が `xref-only`。効かなかったときに
「機構の理解が違う」のか「他の変更に埋もれた」のかを分けられなくなる。

**F5 は束ねてはいけない** — 軸を動かすと隣接セルの意味が全部変わる。

---

## 7. パラメータ表

`bank` は slave = フル BIN で `0x88000+XDF`、master = `XDF`。パーシャル BIN では XDF＝オフセット。

| パラメータ定義名 | 種別 | XDFアドレス | ファイルオフセット | bank | 現在値 | 単位 | 役割 | 変更方向・量 | リスク | 根拠 |
|---|---|---|---|---|---|---|---|---|---|---|
| K_SMG_J_MOTOR | constant | 0x280A | 0x8A80A | slave | 0.25 | Nms2 | 指令スルーレート上限クランプ `min(指令, T/J)`。**再係合レギュレータからも呼ばれる** | 症状が出たらまず純正 0.25 へ。以後 raw 30/28/27/26 で折り合いを探す。**J_new に一致させるのが目的ではない** | 小さすぎ＝再係合が荒い／大きすぎ＝ブリップ鈍化 | code-confirmed |
| K_MD_J_MOTOR | constant | 0x9554 | 0x09554 | master | 0.268657 | Nms2 | **16 bit。** ソフトレブリミッタ I 項で生存（md_max_begr 経由は不活性） | = J_new を直接 | レブリミッタの当たり方のみ | code-confirmed |
| KL_SMG_MOT_J_MOTOR | curve | 0x2ACC | 0x8AACC | slave | 0.0078 / 0.398 / 0.5 | Nms2 | オブザーバ内の慣性名義の項。軸は**目標**回転 | **変更しない** | 用途未回収で変更の向きすら不明 | 参照は code-confirmed / 用途は未回収 |
| KL_MD_JFZ_GANG | curve | 0x955E | 0x0955E | master | 0 / 0.701 / 1.601 / 3.399 / 5.0 / 8.0 / 12.0 / 0.75 | Nms2 | **BMW 自身のクランク換算車両慣性。** r_eff の分母 | **読み取り専用** | — | code-confirmed |
| K_LFR_TAU_IA1 | constant | 0x9A9E | 0x09A9E | master | 5.12（raw 1 ＝ 最も遅い端） | s | アイドル I 項時定数。`5.12/x` の逆数 | **1.707 (raw 3)**、効かなければ 1.024 (raw 5)。r と無関係に端に張り付いている | 速くしすぎでハンチング | code-confirmed |
| K_LFR_TAU_IA2_KKS | constant | 0x9A9F | 0x09A9F | master | 0.0201（raw 255 ＝ 最も速い端） | s | クラッチ断時の I 項時定数。20 ms タスクで τ=20 ms は 1 周期で全捨て | **0.256 (raw 20)** | 復帰が粘る | code-confirmed |
| K_LFR_TAU_IA2_KS | constant | 0x9A4E | 0x09A4E | master | 0.32 | s | クラッチ接時の対応値 | **変更しない**（IA2_KKS の外れ具合を示す基準） | — | code-confirmed |
| KL_LFR_TZ_NEG | curve | 0x9CE6 | 0x09CE6 | master | **16 点すべて 0.0** | Nm | 回転を下げる側の点火権限。**存在しない** | 0/1.0/2.0/3.5/6.0/7.0/8.0/8.5/以降 9.0。`KL_MD_RES_LRW` と**必ず同時** | 常時リザーブ＝常時リタード | xref-only |
| KL_MD_RES_LRW | curve | 0x97D8 | 0x097D8 | master | y[0] = 0.0 | Nm | 上のゲート | y[0] を **3.0** | 3 Nm 超で燃費・排気温・発進が悪化 | xref-only |
| KL_LFR_DQP_POS | curve | 0x9ABE | 0x09ABE | master | 0–60 rpm 帯が 0 | Nm | アイドル P 項。低偏差側が不感帯 | 20 rpm 点 0.4 / 40 rpm 点 1.0 | 微小偏差で介入＝ハンチング | code-confirmed |
| KL_SA_N40_GANG | curve | 0x0E3C | 0x88E3C | slave | index 0 = 320 | rpm | 燃料カット下限（ギア別）。**index 0（クラッチ切/N）だけが対象** | **× 1/r**、40 rpm 刻み。in-gear は動かさない | 復帰が早まり燃費悪化 | code-confirmed |
| KL_SA_N40_HYS_GANG | curve | 0x0E2A | 0x88E2A | slave | index 0 = 120 | rpm | 同ヒステリシス | 同上 | 同上 | code-confirmed |
| KL_SA_N40_DN40 | curve | 0x0E58 | 0x88E58 | slave | x −200/−320/−400/−1600 | rpm/s → rpm | 勾配による先読み。**x 軸が勾配なので本当に慣性でスケールする** | 軸を × 1/r。**最後に、単独で** | 軸移動は隣接セルの意味を変える | code-confirmed |
| K_WE_DN40_HARD | constant | 0x0E1E | 0x88E1E | slave | **−5000**（下限 −5120） | rpm/s | hard/soft 分岐 | **変更しない — 既に端**。「−8000 にする」は物理的に書けない | — | code-confirmed |
| K_N_TAU_DN | constant | 0xC419 | 0x0C419 | master | 0.0985 | s | 回転勾配 PT1。`2.56/x` の逆数 | **× r**。位相余裕の回復用。`D_N40` には効かない | 速くしすぎるとノイズが制御に入る | code-confirmed |
| KL_DYN_TZ_DBGR | curve | 0xB388 | 0x0B388 | master | 5000 rpm 以上 −12.0 | °KW | 過渡点火リタード | 高回転側を緩める（例 −9.0） | 戻しすぎで過渡がドンとくる | code-confirmed |
| K_SMG_N_ZIEL_ABWUERG | constant | 0x2855 | 0x8A855 | slave | 1200 | rpm | SMG エンスト回避目標 | **× 1/√r**（`0.5·J·ω²` なので平方根）。40 rpm 刻み | 発進時の回転が上がる | funktionsrahmen-only |
| ティップイン段別係数 | series(16bit×8, `/1024`) | **0x927C（XDF 未定義）** | 0x0927C | master | 1.00 / **0.75** / 0.80 / 0.90 / 1.00… | — | `MD_LS_DELTA` のギア倍率。**ティップイン鎖で唯一ギア軸を持つ** | 各段に `r_eff(g)` を乗算 | 直接パッチ。**バイト数・アドレスを二重確認** | code-confirmed |
| ダッシュポット段別係数 | series(16bit×8, `/1024`) | **0x929E（XDF 未定義）** | 0x0929E | master | 1.00 / **0.60** / 0.84 / 0.91 / 0.93 / 1.00 | — | `MD_DP_DELTA` のギア倍率 | 同上 | 同上。エンブレ立ち上がりが鈍る | code-confirmed |
| KF_MD_LS_KOMF | map 6×6 | 0x9396 | 0x09396 | master | 最小 1.7（1500 rpm × 100 Nm） | Nm/10ms | ティップイン許容変化率。**大きい＝ダイレクト＝シャクリやすい** | **一律スケールしない。** ログが指したセルのみ | 全面を下げると踏み始めが一拍遅れる | code-confirmed |
| KF_MD_DASHPOT_FAKTOR | map 4×3 | 0x946A | 0x0946A | master | 1000 rpm 列 0.200/0.400/0.700/1.000 | — | ダッシュポット係数。**イベント中ラッチ**、参照軸は生の要求 | 段別係数で足りなければ 1000 rpm 列に `r_eff(1..2)` | 戻りが緩くなりすぎる | code-confirmed |

### 触ってはいけないもの

| 対象 | 理由 |
|---|---|
| `K_MD_LS_CONFIG`（master `0x91DC` = 1） | LS 横軸の絶対/相対切替。マップの意味が変わり表の作り直しが要る |
| `K_MD_DF_NMIN`(`0x91E4`) / `K_MD_DF_VMIN`(`0x91E6`) | フィルタのバイパス条件そのもの。**§2.2 の測定手順の前提が崩れる** |
| `K_SMG_SK_*`（`0x28AE`–`0x28B4`）、`smg_clutch_protection_monitor` 関連 | 安全概念・クラッチ保護 |
| **`K_SMG_R_RAD_DYN`(`0x280C`)** | `K_SMG_J_MOTOR`(`0x280A`) の**隣（+2）**。この表で最も誤爆しやすい |
| `K_LLS_TV_MIN`(`0x9DAE`) / `_MAX`(`0x9DAC`) | アイドル弁の保護クランプ |
| すべてのテーブルの軸（`KL_SA_N40_DN40` を除く） | 隣接セルの意味が変わる |

---

## 8. 検証ログと合否基準

| 群 | 操作 | 見る信号 | 合否 |
|---|---|---|---|
| F1 | 発進・変速直後の踏み足し ×5 | `n40`, `md_ind_ne`, `gang`, `s_krafts` | 再係合時の `n40` オーバーシュートが減る。体感でガク付き消失 |
| F2（アイドル） | 3000 rpm から N・アクセルオフ／ギア入りでクラッチ断 | `n40` | アイドル復帰時間の短縮、断時の落ち込み量の減少 |
| F2（SA/WE） | クラッチを切って惰行 | `sa_we_st`, `n40` | 二段しゃくりの消失 |
| F3 | ブリップ後の回転落ち | `n40` | **`xref-only` なのでログで確認するまで成功と見なさない** |
| F4 | 2 速 1500–2000 rpm で 1/3 踏み増し | **`md_dyn_st` bit6**, `md_ind_wunsch`, `md_ind_ne` | bit6 が立つ区間で `wunsch − ne` の段差が縮む |
| 全群 | 各群の後 | block 179 `lu[0..5]` | 失火マージンの再確認。最初に出やすい DTC は **D5** |

> **`md_dyn_st` bit6 がティップイン制限の唯一の直接証拠である。**
> `MD_LS_DELTA`(`0xFFD8B8`) は 1 MB 全域で書き込み 2 箇所のみ・読み手ゼロの観測専用値で、
> **どのブロックにも載っていない**。

---

## 9. 実装

| 何 | どこ |
|---|---|
| ブロック 83 の定義 | `src/lib/dme-link/liveValueBlocks.ts`（`EGAS_MEASUREMENT_BLOCK`） |
| 慣性ログのサンプル型 | `src/lib/dme-link/types.ts`（`EgasMeasurement` — VE ログとは別の型） |
| 勾配の補正 | `src/lib/inertia/gradient.ts`（`correctDN40`、飽和判定、中心差分） |
| J 推定 | `src/lib/inertia/estimator.ts` |
| 既知 J の模擬エンジン | `src/lib/inertia/bench.ts`（PRACTICE モードでも使う） |
| クラス分けと補正生成 | `src/lib/inertia/corrections.ts` |
| パラメータ台帳 | `src/lib/ecu-items/catalog/inertia.ts` |
| 系譜チェック | `src/lib/lineage/preflight.ts` |
| 検証 | `npm run verify:inertia`（40 項目） |

---

## 10. 確立できなかったこと

| 項目 | 状態 |
|---|---|
| **`md_ind_opt_korr` の絶対確度** | モデル出力なので乗算誤差 `k` が J に 1:1 で乗る。自由降下の検算でも約分されて検出できない。`M_loss` の物理的妥当性で ±30 % 程度にしか縛れない（§4.3） |
| **DMF ダンパ喪失の定量** | 4–10 Hz は DS2 で測れない。加速度計を付けるか、`KF_MD_LS_KOMF` の症状セルを実走で詰めるかの二択 |
| **`KL_SMG_MOT_J_MOTOR` の用途** | `smg_mot_j_motor` を引いていることは code-confirmed だが、オブザーバが結果をどう使うかは未回収 |
| **`0x927C` / `0x929E` の XDF 名** | XDF に定義が無い。アドレスと形状のみ確認 |
| **`inertia` プロファイルの実測レート** | 未実測。**理論値を書かない** — 60 s ログのタイムスタンプ中央値差から出して、ここに記録すること |
| **MT 車での有効性** | SMG 経路は `cfg_s.getriebetyp == 0x40` でゲートされ、`smg_can_istgang` を CAN から取る。フレームが無ければ SMG 側は死にコード |

---

## 参照

- `45-drivability-filters.md` — ティップイン／ティップアウトの機構（`MD_LS_DELTA` の組み立て、`md_dyn_st`）
- `80-smg-shift-tuning.md` — `K_SMG_J_MOTOR` のレートクランプ、SMG 変速
- `30-idle-control.md` — アイドル制御全体
- `CSL_0401_Binary_Disassembly_Notes/docs/lightweight_flywheel_tuning.md` — 慣性低下が壊すもの全体（331 行のパラメータ表）
- `CSL_0401_Binary_Disassembly_Notes/docs/finding_xdf_undefined_calibration.md` — XDF 未定義較正の扱い
